<h1>All Projects</h1>

<% @projects.each do |project| %>
  <p>
    <strong><%= link_to project.name, project_path(project) %></strong> — ₹<%= project.amount %>
  </p>
<% end %>

<%= link_to "New Project", new_project_path %>